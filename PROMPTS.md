# Prompts Used

This file contains the prompts used to generate the code for this project.


Make the chat box go to the rightand create another box for my chess game so it stays side by side with the text, on its left;


Create a 2 player chess game on the chess area just created, where it will store the state of the game on a matrix 8x8 (chess grid) where the pawns are labeled as P (pawns), rook as R, knights as K, bishops as B, queen as Q and king as KNG. The matrix has to store the color of the piece so it should put a prefix of the color before the name of the piece itself (Ex: BB) black bishop, (WKNG) white king etc.;


Implement the check system, and I should not be able to eat the king. There should be someone that wins, and when that happens there should be a button to reset the game


The checks are not working, i can play pieces that leave the king on check on checkmate its not showing a victory message.


whats the variable that has the matrix? I want to have it on a string, so I can pass it to chat.js


Add numbers and letters to the grid, only on the first row and collumn, on vertical its numbers from 1 to 8 and horizontal its a to h